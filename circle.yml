dependencies:

  cache_directories:
    - elm-stuff/build-artifacts/

  pre:
    - npm install -g elm
    - npm install -g elm-test

  override:
    - bundle install --jobs=4
    - elm make --yes

test:

  override:
    - bundle exec jekyll build
    - elm test
