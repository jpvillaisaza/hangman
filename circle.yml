dependencies:

  cache_directories:
    - elm-stuff/build-artifacts/

  pre:
    - npm install -g elm

  override:
    - bundle install --jobs=4

test:

  override:
    - elm make --yes
    - bundle exec jekyll build
